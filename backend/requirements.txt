# Backend dependencies (pinned) — Phase 1 Backend-Fundament
# Web framework / ASGI
fastapi==0.138.1
starlette==1.3.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# Persistence / migrations
SQLAlchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.12          # PostgreSQL driver (runtime); SQLite is built-in for tests

# Configuration / validation
pydantic==2.13.4
pydantic-settings==2.14.2

# Auth / security (used from Phase 2 onward; hashing already needed for seed users)
bcrypt==5.0.0
python-jose[cryptography]==3.5.0

# Tests
pytest==9.1.1
httpx==0.28.1
